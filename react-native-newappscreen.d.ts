declare module 'react-native/Libraries/NewAppScreen' {
  // Minimal declarations for NewAppScreen helpers used in the template.
  export const Colors: {
    lighter?: string;
    white?: string;
    black?: string;
    dark?: string;
  };

  // These are React components or functions used in the example template.
  // Keep them as any to avoid strict typing issues; feel free to provide
  // more specific types if you need autocompletion later.
  export const DebugInstructions: any;
  export const Header: any;
  export const LearnMoreLinks: any;
  export const ReloadInstructions: any;

  const _default: any;
  export default _default;
}
